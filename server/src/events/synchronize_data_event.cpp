#include <server/core/events/synchronize_data_event.hpp>

void SynchronizeDataEvent::perform(ManagerContext &context)
{
    // TODO: zaimplementować — przesłać aktualny stan kanałów i wiadomości do klienta
    (void)context;
}
